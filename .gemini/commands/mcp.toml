description = "Manage MCP servers for firmware development"
prompt = """
Manage Model Context Protocol (MCP) servers for Momentum Firmware development.

Current MCP configuration:
!{grep -A20 "mcpServers" .gemini/settings.json 2>/dev/null || echo "No MCP servers configured"}

Available setup options:
- GitHub integration for issue/PR management
- Custom MCP servers for firmware tools

Setup instructions: {{args}}
"""